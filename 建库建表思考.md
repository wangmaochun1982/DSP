# 一、公共维度库 + 业务主题库 + 应用

# 架构规划与库名设计

我们在生产环境创建 3 个数据库：

dim_db：公共维度库（存放用户、商品等全域共享维度表）。

trade_db：交易业务主题库（存放交易域的 ODS、DWD、DWS 层数据表）。

ads_db：应用数据库（专门对接前端 BI 报表与大屏数据）。

# 一、公共维度库（dim_db）
维度表数据量通常中小规模，但对主键更新与查询性能要求高，因此使用 UNIQUE 唯一主键模型，并开启 Merge-on-Write（写时合并）。

1. 用户维度表（dim_user_info_df）

```sql
CREATE DATABASE IF NOT EXISTS dim_db;

CREATE TABLE IF NOT EXISTS dim_db.dim_user_info_df (
    user_id          BIGINT NOT NULL COMMENT "用户ID",
    user_name        VARCHAR(100) NOT NULL COMMENT "用户名",
    gender           TINYINT COMMENT "性别: 1男 2女 0未知",
    age              INT COMMENT "年龄",
    city_code        VARCHAR(20) COMMENT "城市编码",
    register_time    DATETIME COMMENT "注册时间",
    is_vip           TINYINT DEFAULT "0" COMMENT "是否VIP: 0否 1是",
    updated_at       DATETIME COMMENT "维度更新时间"
) ENGINE=OLAP
UNIQUE KEY(user_id)
DISTRIBUTED BY HASH(user_id) BUCKETS 10
PROPERTIES (
    "replication_num" = "3",
    "enable_unique_key_merge_on_write" = "true", -- 开启写时合并，显著提升读性能/Join性能
    "compression" = "ZSTD"                       -- 使用 ZSTD 压缩算法降低存储成本
);
```

# 二、交易主题库（trade_db）
包含了数据仓库核心链路的 ODS -> DWD -> DWS 三层。

```SQL
CREATE DATABASE IF NOT EXISTS trade_db;
```

1. ODS 层：订单原始增量表（ods_trade_orders_di）
特点：接收上游 CDC（如 Flink-CDC / Canal）同步过来的 MySQL 原始数据。使用 UNIQUE 模型支持按订单主键实时更新。

```SQL
CREATE TABLE IF NOT EXISTS trade_db.ods_trade_orders_di (
    order_id         BIGINT NOT NULL COMMENT "订单ID",
    order_sn         VARCHAR(64) NOT NULL COMMENT "订单编号",
    user_id          BIGINT NOT NULL COMMENT "买家ID",
    order_status     TINYINT NOT NULL COMMENT "订单状态: 1未付款 2已付款 3已发货 4已完成 5已取消",
    total_amount     DECIMAL(18, 4) NOT NULL COMMENT "订单总金额",
    pay_amount       DECIMAL(18, 4) COMMENT "实际支付金额",
    created_at       DATETIME NOT NULL COMMENT "下单时间",
    updated_at       DATETIME NOT NULL COMMENT "记录更新时间",
    ods_create_time  DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT "ODS入库时间"
) ENGINE=OLAP
UNIQUE KEY(order_id)
PARTITION BY RANGE(created_at) () -- 动态分区：按下单时间按天分区
DISTRIBUTED BY HASH(order_id) BUCKETS AUTO
PROPERTIES (
    "replication_num" = "3",
    "enable_unique_key_merge_on_write" = "true",
    "dynamic_partition.enable" = "true",
    "dynamic_partition.time_unit" = "DAY",
    "dynamic_partition.start" = "-30",  -- 自动保留最近 30 天分区
    "dynamic_partition.end" = "3",      -- 提前创建未来 3 天分区
    "dynamic_partition.prefix" = "p"
);
```

2. DWD 层：订单与明细事实表（dwd_trade_order_detail_di）
特点：清洗后的明细事实表，采用 DUPLICATE 明细模型。数据量极大，开启动态分区；采用 Colocate Join 组设置，确保订单主表与子表关联查询时无跨网络数据传输。

```sql
CREATE TABLE IF NOT EXISTS trade_db.dwd_trade_order_detail_di (
    dt               DATE NOT NULL COMMENT "分区日期(与order_time一致)",
    order_id         BIGINT NOT NULL COMMENT "订单ID",
    item_id          BIGINT NOT NULL COMMENT "订单明细ID",
    user_id          BIGINT NOT NULL COMMENT "买家ID",
    product_id       BIGINT NOT NULL COMMENT "商品ID",
    category_id      INT NOT NULL COMMENT "商品类目ID",
    sku_price        DECIMAL(18, 4) NOT NULL COMMENT "单价",
    sku_num          INT NOT NULL COMMENT "购买数量",
    split_pay_amount DECIMAL(18, 4) NOT NULL COMMENT "分摊实付金额",
    order_time       DATETIME NOT NULL COMMENT "下单时间"
) ENGINE=OLAP
DUPLICATE KEY(dt, order_id, item_id) -- 前缀索引优化：频繁过滤的字段放在最前
PARTITION BY RANGE(dt) ()
DISTRIBUTED BY HASH(order_id) BUCKETS AUTO
PROPERTIES (
    "replication_num" = "3",
    "colocate_with" = "trade_order_group", -- Colocate 优化：与同组表本地 Join
    "dynamic_partition.enable" = "true",
    "dynamic_partition.time_unit" = "DAY",
    "dynamic_partition.start" = "-180",
    "dynamic_partition.end" = "3",
    "dynamic_partition.prefix" = "p"
);
```


3. DWS 层：用户每日消费汇总表（dws_trade_user_sales_1d）
特点：按“天 + 用户”粒度的轻度汇总表，选用 AGGREGATE 预聚合模型，提升分析类查询性能。

```sql

CREATE TABLE IF NOT EXISTS trade_db.dws_trade_user_sales_1d (
    dt               DATE NOT NULL COMMENT "统计日期",
    user_id          BIGINT NOT NULL COMMENT "用户ID",
    order_count      BIGINT SUM DEFAULT "0" COMMENT "当日下单笔数",
    total_sku_num    BIGINT SUM DEFAULT "0" COMMENT "当日购买商品件数",
    total_pay_amount DECIMAL(18, 4) SUM DEFAULT "0" COMMENT "当日消费总金额"
) ENGINE=OLAP
AGGREGATE KEY(dt, user_id)
PARTITION BY RANGE(dt) ()
DISTRIBUTED BY HASH(user_id) BUCKETS AUTO
PROPERTIES (
    "replication_num" = "3",
    "dynamic_partition.enable" = "true",
    "dynamic_partition.time_unit" = "DAY",
    "dynamic_partition.start" = "-365", -- 汇总表保存历史较长
    "dynamic_partition.end" = "3",
    "dynamic_partition.prefix" = "p"
);
```

# 三、应用层库（ads_db）
直接对接前端 API / 网关或 BI 报表系统，注重并发读取和极速响应。

```sql
CREATE DATABASE IF NOT EXISTS ads_db;
```

1. ADS 层：核心指标看板日汇总表（ads_sales_daily_kpi）
特点：使用 UNIQUE 模型（支持每天 ETL 任务重跑时的覆盖写入），分桶数设置小而精。

```SQL
CREATE TABLE IF NOT EXISTS ads_db.ads_sales_daily_kpi (
    dt                DATE NOT NULL COMMENT "统计日期",
    channel_code      VARCHAR(32) NOT NULL COMMENT "渠道编码",
    pv                BIGINT COMMENT "访问量",
    uv                BIGINT COMMENT "独立访客数",
    order_user_cnt    BIGINT COMMENT "下单人数",
    order_cnt         BIGINT COMMENT "总订单数",
    gmv_amount        DECIMAL(18, 4) COMMENT "GMV金额",
    pay_amount        DECIMAL(18, 4) COMMENT "实际成交金额",
    updated_at        DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT "指标计算时间"
) ENGINE=OLAP
UNIQUE KEY(dt, channel_code)
DISTRIBUTED BY HASH(channel_code) BUCKETS 5
PROPERTIES (
    "replication_num" = "3",
    "enable_unique_key_merge_on_write" = "true"
);
```

# 四、生产环境建表 Checklist（核对清单）
在准备提交 SQL 脚本创建生产环境表之前，请对照以下规则进行检查：

1.主键与模型选择

需要按主键去重或更新的（如 CDC 同步表、维度表、ADS 覆盖写表）：使用 UNIQUE KEY + enable_unique_key_merge_on_write = true。

纯事实明细表：使用 DUPLICATE KEY。

固定预聚合指标表：使用 AGGREGATE KEY。

2.前缀索引字段定义（排序键）

将 WHERE / JOIN 中最常用的过滤字段放在字段定义列表的最前面。

3.分区与分桶配置

超过 1000 万行的数据表必须做分区（推荐按天 dt 或按月 yyyyMM 分区），并配置 dynamic_partition 自动调度。

分桶建表推荐采用 BUCKETS AUTO，或确保单分桶 (Tablet) 大小控制在 100MB ~ 10GB。

4.高可用与性能

生产环境必须开启 replication_num = 3。

精确金额列统一采用 DECIMAL(18, 4)，禁止用 FLOAT/DOUBLE。


 


 ###  二、按各个数据分层单独建库（ODS、DWD、DWS、ADS、DIM 各自为一个独立数据库），同样是一种非常经典的架构建库方式。

1. 这种建库模式的优缺点分析

优点架构极度清晰：数据流向非常直观（ods_db -> dwd_db-> dws_db ->ads_db），运维人员和数仓工程师一眼就能看清数据处于什么阶段。

2.权限隔离极其简单：

下游 BI 报表 / API 系统账号：只分配 ads_db 的只读权限。

分析师 / Data Scientist：分配 dwd_db / dws_db 的只读权限，绝不能动 ods_db。

ETL 调度账号：分配各库的写权限。


# 在这套方案中，每一个分层都是一个独立数据库，表名通常不需要再加分层前缀（或者保留前缀以增强可读性）。

第一步：创建 5 个分层数据库

```sql
CREATE DATABASE IF NOT EXISTS ods_db;  -- 原始数据层
CREATE DATABASE IF NOT EXISTS dim_db;  -- 公共维度层
CREATE DATABASE IF NOT EXISTS dwd_db;  -- 明细数据层
CREATE DATABASE IF NOT EXISTS dws_db;  -- 汇总数据层
CREATE DATABASE IF NOT EXISTS ads_db;  -- 应用数据层

```

 

第二步：各库建表落地规范
1. 维度库（dim_db）：用户维度表

```SQL
CREATE TABLE IF NOT EXISTS dim_db.dim_user_info (
    user_id          BIGINT NOT NULL COMMENT "用户ID",
    user_name        VARCHAR(100) NOT NULL COMMENT "用户名",
    gender           TINYINT COMMENT "性别: 1男 2女 0未知",
    city_code        VARCHAR(20) COMMENT "城市编码",
    register_time    DATETIME COMMENT "注册时间",
    updated_at       DATETIME COMMENT "更新时间"
) ENGINE=OLAP
UNIQUE KEY(user_id)
DISTRIBUTED BY HASH(user_id) BUCKETS 10
PROPERTIES (
    "replication_num" = "3",
    "enable_unique_key_merge_on_write" = "true" -- Unique模型必配：写时合并提升读性能
);
```

2. ODS 库（ods_db）：订单原始同步表
```SQL
CREATE TABLE IF NOT EXISTS ods_db.ods_trade_orders (
    order_id         BIGINT NOT NULL COMMENT "订单ID",
    order_sn         VARCHAR(64) NOT NULL COMMENT "订单编号",
    user_id          BIGINT NOT NULL COMMENT "买家ID",
    order_status     TINYINT NOT NULL COMMENT "订单状态",
    total_amount     DECIMAL(18, 4) NOT NULL COMMENT "订单总金额",
    pay_amount       DECIMAL(18, 4) COMMENT "实际支付金额",
    created_at       DATETIME NOT NULL COMMENT "下单时间",
    updated_at       DATETIME NOT NULL COMMENT "更新时间",
    ods_create_time  DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT "ODS入库时间"
) ENGINE=OLAP
UNIQUE KEY(order_id)
PARTITION BY RANGE(created_at) ()
DISTRIBUTED BY HASH(order_id) BUCKETS AUTO
PROPERTIES (
    "replication_num" = "3",
    "enable_unique_key_merge_on_write" = "true",
    "dynamic_partition.enable" = "true",
    "dynamic_partition.time_unit" = "DAY",
    "dynamic_partition.start" = "-30",
    "dynamic_partition.end" = "3",
    "dynamic_partition.prefix" = "p"
);
```

3. DWD 库（dwd_db）：交易明细事实表
```SQL
CREATE TABLE IF NOT EXISTS dwd_db.dwd_trade_order_detail (
    dt               DATE NOT NULL COMMENT "分区日期",
    order_id         BIGINT NOT NULL COMMENT "订单ID",
    item_id          BIGINT NOT NULL COMMENT "明细ID",
    user_id          BIGINT NOT NULL COMMENT "买家ID",
    product_id       BIGINT NOT NULL COMMENT "商品ID",
    sku_price        DECIMAL(18, 4) NOT NULL COMMENT "单价",
    sku_num          INT NOT NULL COMMENT "购买数量",
    split_pay_amount DECIMAL(18, 4) NOT NULL COMMENT "分摊实付金额",
    order_time       DATETIME NOT NULL COMMENT "下单时间"
) ENGINE=OLAP
DUPLICATE KEY(dt, order_id, item_id) -- Duplicate明细模型：过滤频繁的字段放在最前作为前缀索引
PARTITION BY RANGE(dt) ()
DISTRIBUTED BY HASH(order_id) BUCKETS AUTO
PROPERTIES (
    "replication_num" = "3",
    "dynamic_partition.enable" = "true",
    "dynamic_partition.time_unit" = "DAY",
    "dynamic_partition.start" = "-180",
    "dynamic_partition.end" = "3",
    "dynamic_partition.prefix" = "p"
);
```
4. DWS 库（dws_db）：用户每日消费汇总表
```SQL
CREATE TABLE IF NOT EXISTS dws_db.dws_trade_user_sales_1d (
    dt               DATE NOT NULL COMMENT "统计日期",
    user_id          BIGINT NOT NULL COMMENT "用户ID",
    order_count      BIGINT SUM DEFAULT "0" COMMENT "当日下单笔数",
    total_pay_amount DECIMAL(18, 4) SUM DEFAULT "0" COMMENT "当日消费总金额"
) ENGINE=OLAP
AGGREGATE KEY(dt, user_id) -- Aggregate预聚合模型
PARTITION BY RANGE(dt) ()
DISTRIBUTED BY HASH(user_id) BUCKETS AUTO
PROPERTIES (
    "replication_num" = "3",
    "dynamic_partition.enable" = "true",
    "dynamic_partition.time_unit" = "DAY",
    "dynamic_partition.start" = "-365",
    "dynamic_partition.end" = "3",
    "dynamic_partition.prefix" = "p"
);
```
5. ADS 库（ads_db）：核心 KPI 指板日汇总表
```SQL
CREATE TABLE IF NOT EXISTS ads_db.ads_sales_daily_kpi (
    dt                DATE NOT NULL COMMENT "统计日期",
    channel_code      VARCHAR(32) NOT NULL COMMENT "渠道编码",
    gmv_amount        DECIMAL(18, 4) COMMENT "GMV金额",
    pay_amount        DECIMAL(18, 4) COMMENT "实际成交金额",
    updated_at        DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT "指标计算时间"
) ENGINE=OLAP
UNIQUE KEY(dt, channel_code)
DISTRIBUTED BY HASH(channel_code) BUCKETS 5
PROPERTIES (
    "replication_num" = "3",
    "enable_unique_key_merge_on_write" = "true"
);
```



# 那还有一种

```text

Doris 集群
├── dim_db (公共维度库 - 单独独立)
│    ├── dim_user_info           -- 全局公共：用户主维表
│    ├── dim_product_info        -- 全局公共：商品主维表
│    └── dim_geography           -- 全局公共：行政区划/城市维表
│
├── trade_db (交易业务域库)
│    ├── dim_trade_pay_type      -- 局部维度：交易域特有的支付渠道维表
│    ├── dim_trade_refund_reason -- 局部维度：交易域特有的退款原因维表
│    ├── dwd_trade_order_detail  -- 事实表
│    └── dws_trade_user_sales_1d -- 汇总表
│
└── supply_chain_db (供应链业务域库)
     ├── dim_warehouse_info      -- 局部维度：供应链特有的仓库配置维表
     └── dwd_stock_in_detail     -- 事实表
```



```text

【行存物理文件 (如 MySQL InnoDB Page / CSV)】
[ 行1: id, name, age ]  [ 行2: id, name, age ]  [ 行3: id, name, age ]
  └─────────── 字节连续 ───────────┘

----------------------------------------------------------------------

【Doris 列存物理文件 (Segment)】
[ Header ]
[ 所有行的 id 列连续保存在这里:   id1, id2, id3, ... ] 
[ 所有行的 name 列连续保存在这里: name1, name2, name3, ... ]
[ 所有行的 age 列连续保存在这里:  age1, age2, age3, ... ]
[ Footer (记录各列在文件里的字节起始偏移量) ]
```
